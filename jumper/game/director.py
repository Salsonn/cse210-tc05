from game.parachute import Parachute
from game.word import Word
from game.console import Console

class Director:

    def __init__(self)
        parachute = Parachute()
        word = Word()
        console = Console()